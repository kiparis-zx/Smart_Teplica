# Управление теплицей через веб-интерфейс

Система для удаленного мониторинга и управления микроклиматом теплицы через веб-интерфейс.


## Установка и запуск

### 1. Установка зависимостей Node.js
```bash
npm install
```

### 2. Настройка COM порта
В файле `teplica.js` измените номер COM порта на ваш:
```javascript
const port = new SerialPort({
    path: 'COM3', // Измените на ваш COM порт
    baudRate: 9600
});
```

### 3. Запуск сервера
```bash
npm start
```

### 4. Доступ к интерфейсу
Откройте браузер и перейдите по адресу: http://localhost:3000

## Структура проекта

```
project_TEPLICA/
├── teplica.ino          # Код для Arduino
├── teplica.js           # Node.js сервер
├── package.json         # Зависимости проекта
├── templates/
│   └── index.html       # Веб-интерфейс
├── static/
│   └── js/
│       └── translations.js # Файл с переводами
└── README.md           # Документация
```

## Команды управления

### Сервопривод:
- `close` - закрыть окно
- `open` - открыть окно на 110°
- `open90` - открыть окно на 90°

### Насос:
- `on` - включить насос
- `off` - выключить насос

## API эндпоинты

- `GET /api/sensors` - получить данные с датчиков
- `POST /api/command` - отправить команду на Arduino

## WebSocket события

- `sensorData` - получение данных с датчиков в реальном времени
- `command` - отправка команд на Arduino

## ✨ Особенности

- **🖥️ Современный веб-интерфейс**: Адаптивный дизайн с анимированным фоном, вкладками и визуализацией данных в реальном времени.
- **📊 Мониторинг в реальном времени**: Отслеживание влажности почвы, освещенности и температуры с помощью `Socket.IO`.
- **🕹️ Ручное и автоматическое управление**:
    - **Сервопривод**: Управление окном (открыть/закрыть).
    - **Насос**: Включение/выключение полива.
    - **Светодиод**: Управление дополнительным освещением.
- **⚙️ Гибкие настройки**: Возможность изменять COM-порт, пороги срабатывания датчиков и таймеры через интерфейс.
- **🌐 Многоязычная поддержка**: Интерфейс переведен на русский и английский языки с возможностью переключения.
- **🚀 Простота запуска**: Легкая установка зависимостей и запуск сервера с помощью `npm`.

## Требования

- Node.js 14+
- Arduino с подключенными датчиками
- COM порт для связи с Arduino
