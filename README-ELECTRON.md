# Управление теплицей - Десктопное приложение (Electron)

## 🚀 Установка и запуск

### 1. Установка зависимостей

Сначала установите зависимости для веб-сервера:
```bash
npm install
```

Затем установите зависимости для Electron:
```bash
# Скопируйте package-electron.json в package.json
cp package-electron.json package.json
npm install
```

### 2. Запуск приложения

#### Режим разработки:
```bash
npm run dev
```

#### Обычный запуск:
```bash
npm start
```

### 3. Сборка приложения

Для создания установочных файлов:
```bash
npm run build
```

Готовые файлы будут в папке `dist/`.

## 📁 Структура проекта

```
project_TEPLICA/
├── main.js                 # Главный процесс Electron
├── preload.js             # Предзагрузка для безопасности
├── teplica.js             # Веб-сервер (Node.js)
├── package.json            # Зависимости Electron
├── package-electron.json   # Конфигурация для Electron
├── templates/
│   └── index.html         # Веб-интерфейс
├── static/
│   └── js/
│       └── translations.js # Переводы
├── assets/
│   └── icon.png           # Иконка приложения
└── README-ELECTRON.md     # Эта инструкция
```

## ⚙️ Особенности Electron версии

### 🖥️ Десктопное окно
- Независимое окно приложения
- Минимальный размер: 800x600
- Стандартный размер: 1200x800
- Иконка приложения

### 🔗 Интеграция с сервером
- Автоматический запуск встроенного веб-сервера
- Динамическое определение порта
- Graceful shutdown при закрытии

### 🎨 Меню приложения
- **Файл**: Выход
- **Вид**: Перезагрузка, полноэкранный режим, масштабирование
- **Разработчик**: Инструменты разработчика

### 🔒 Безопасность
- Отключен Node.js в рендерере
- Контекстная изоляция
- Безопасная связь через preload.js

## 🐛 Отладка

Для открытия инструментов разработчика:
- Через меню: Разработчик → Toggle Developer Tools
- Или используйте `Ctrl+Shift+I` (Windows/Linux) или `Cmd+Option+I` (Mac)

## 📦 Сборка для дистрибуции

### Windows
```bash
npm run build
```
Создаст `.exe` установщик в `dist/`.

### macOS
```bash
npm run build
```
Создаст `.dmg` образ в `dist/`.

### Linux
```bash
npm run build
```
Создаст `.AppImage` и `.deb` пакеты в `dist/`.

## 🔧 Настройка COM порта

COM порт настраивается в файле `teplica.js`:
```javascript
const port = new SerialPort({
    path: 'COM3', // Измените на ваш порт
    baudRate: 9600
});
```

## 🌐 Поддержка языков

Приложение поддерживает русский и английский языки. Язык можно изменить в настройках интерфейса.

## 📞 Поддержка

- **Email**: smartgreenhousehelp@gmail.com
- **Разработчик**: powered by kiparis-
